-- A script that  deletes  database hbtn_0c_0 in MySQL server
-- If the database hbtn_0c_0 doesn't exist, your script shouldn't fail
-- Not allowed to use SELECT and SHOW

DROP DATABASE IF EXIST hbtn_0c_0;

-- A script that deletes the database hbtn_0c_0 if it exists

-- Drop (delete) the database if it exists

DROP DATABASE IF EXISTS hbtn_0c_0;
